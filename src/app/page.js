"use client";

// Phase 0 placeholder — confirms the Next.js + Tailwind scaffold renders.
// The full workspace UI is assembled in later phases.
export default function Page() {
  return (
    <main className="min-h-screen flex items-center justify-center p-8">
      <div className="card p-8 max-w-md text-center">
        <h1 className="text-2xl font-bold mb-2">LoanLens</h1>
        <p className="text-muted">
          EMI Workspace scaffold is running. Building features next.
        </p>
      </div>
    </main>
  );
}
